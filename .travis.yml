sudo:     false
language: erlang
install:  "true" # don't let travis run get-deps
script:   "rebar3 do xref, dialyzer, eunit"
otp_release:
    - 18.2
    - 19.3
cache:
  directories:
  - _build
  - .cache/rebar3
